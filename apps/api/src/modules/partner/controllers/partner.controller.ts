// Re-export from new api layer location.
// This shim preserves any external import paths that reference controllers/partner.controller.
export { PartnerController } from '../api/partner.controller';
